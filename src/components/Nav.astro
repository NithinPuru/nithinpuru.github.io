---
const links = [
  { id: "about", label: "About" },
  { id: "experience", label: "Experience" },
  { id: "education", label: "Education" },
  { id: "awards", label: "Awards" },
  { id: "work", label: "Work" },
  { id: "publications", label: "Publications" },
  { id: "finance", label: "Finance" },
  { id: "cv", label: "CV" },
  { id: "contact", label: "Contact" },
];
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<header class="site-nav">
  <div class="site-nav__bar">
    <a class="site-nav__brand" href={`${base}/`} aria-label="Nithin Purushothama — back to top">
      <span class="site-nav__name">Nithin&nbsp;Purushothama</span>
      <span class="site-nav__sub" aria-hidden="true">Analog/Mixed-Signal IC Design</span>
    </a>
    <nav class="site-nav__links" aria-label="Sections">
      {
        links.map((l) => (
          <a href={`${base}/#${l.id}`} data-nav>
            {l.label}
          </a>
        ))
      }
      <a href={`${base}/notebook`} class="site-nav__nb">NB</a>
    </nav>
  </div>
</header>
